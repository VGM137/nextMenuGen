import React from 'react';
import { connect } from 'react-redux'

const CreateOrder = () => {

  return (
    <> 
      <h1 style={{color:'white'}}>Aquí se creará una orden</h1>
    </>
  )
};

export default connect(null, null)(CreateOrder);
